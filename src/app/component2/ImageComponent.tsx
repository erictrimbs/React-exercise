import React from 'react';

const ImageComponent = () => {
  return <img src="/app/assets/balloon.png" alt="Loaded from assets" style={{ width: '100%', height: 'auto' }} />;
};

export default ImageComponent;
